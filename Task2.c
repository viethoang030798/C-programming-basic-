/* Use a loop to take 10 words, and concatenate them into one string  separated by \n. 
Finally print the whole string by one printf() function. */
#include <iostream>

int main(void){
   int i;
   char w[200];
   for(i=0;i<=10;i++)
   {
     printf("Type word: ");
     fget(w,200,stdin);
     fget
   }
}